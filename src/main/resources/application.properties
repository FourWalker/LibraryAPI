spring.application.name=LibraryAPI

spring.h2.console.enabled=true
spring.jpa.defer-datasource-initialization=true
spring.jpa.hibernate.ddl-auto=update
#spring.datasource.url=jdbc:h2:mem:testdb
#Uncomment code above and comment all code below to run without database and use jpa persistence
spring.datasource.url=jdbc:mysql://localhost:3307/library
spring.datasource.username=librarian
spring.datasource.password=librarian
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


#run this in docker to create a docker container ready for use together with the connection above.
#docker run --detach --env MYSQL_ROOT_PASSWORD=admin --env MYSQL_USER=librarian --env MYSQL_PASSWORD=librarian --env MYSQL_DATABASE=library --name mysqldocker --publish 3307:3306 mysql:8-oracle